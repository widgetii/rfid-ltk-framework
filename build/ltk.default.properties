deps.dir=${basedir}/deps
ivy.file=${basedir}/ivy.xml
jar.file=${target.dir}/${ant.project.name}-${ltk_version}.jar
module.version=${ltk_version}
classes.dir=${basedir}/bin
compile.debug=on
compile.debuglevel=lines,vars,source
